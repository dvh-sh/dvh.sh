import React                    from 'react';
import { Image as NextUIImage } from '@nextui-org/image';

const LazyImage = (props: any) => {
    return <NextUIImage {...props} />;
};

export default LazyImage;

// path: src/app/component/TextTyper.tsx